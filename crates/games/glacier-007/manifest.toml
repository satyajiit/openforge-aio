[engine]
schema = 2
kind = "glacier2"
config_format = "toml"
dll = "glacier_007_dll.dll"

[game]
id                 = "glacier-007"
display_name       = "007 First Light"
tagline            = "Single-player. Local writes only."
process_names      = ["007FirstLight.exe"]
primary_module     = "007FirstLight.exe"
supported_versions = []
forbidden_services = []
requires_admin     = false
sort_order         = 200
# Tier-2 injected reflection server (crates/glacier-007-dll). Runs the shared
# glacier-host reflection engine in-process and serves it over a named pipe.
dll_file_name      = "glacier_007_dll.dll"

[icon]
# 007 First Light logo, resized to 512x268 (native 1.91:1 aspect preserved).
path = "assets/icon.png"
